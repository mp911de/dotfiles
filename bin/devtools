#!/bin/bash

DOTFILES_DIRECTORY="${HOME}/.dotfiles"

cd ${DOTFILES_DIRECTORY}

source ./lib/help
source ./lib/utils
source ./lib/devtools
source ${DOTFILES_DIRECTORY}/devtools

# Help text
if [[ "$1" == "-h" || "$1" == "--help" ]]; then
    run_help
    exit
fi

run_devtools
